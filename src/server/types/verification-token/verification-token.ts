import type { VerificationToken as PrismaVerificationToken } from 'prisma/prisma-client';

export type VerificationToken = PrismaVerificationToken;
export type CreateVerificationTokenDTO = VerificationToken;
